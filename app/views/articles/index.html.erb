<div class="col-xs-12">
	<% @articles.each do |article| %>
		<div class="well">

			<h3><%= link_to article.title, article_path(article) %></h3>
					
			<br />
			<p>
				<%= article.text %>
			</p>
			<br />
			
			<span style="font-size: 75%; color: #999">Created at <%= article.created_at %>.
				<% if if user_signed_in? then article.author_id == current_user.id end %>
					You can <%= link_to 'edit', edit_article_path(article) %> or <%= link_to 'delete', article_path(article), method: :delete, data: { confirm: 'Are you sure?' } %> it.
				<% end %>
			</span>

		</div>
	<% end %>
</div>